package com.stackroute.giphermanager.controller;

import org.junit.After;
import org.junit.Before;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.autoconfigure.web.servlet.WebMvcTest;
import org.springframework.boot.test.mock.mockito.MockBean;
import org.springframework.test.context.junit4.SpringRunner;
import org.springframework.test.web.servlet.MockMvc;
import java.util.ArrayList;
import java.util.List;

import com.stackroute.giphermanager.model.Gipher;
import com.stackroute.giphermanager.service.GipherService;

@RunWith(SpringRunner.class)
@WebMvcTest
public class GipherControllerTest {
	/*
	 * 
	 * private Gipher gipher; private User user; private List<Gipher> gifList;
	 * private List<String> searchList;
	 * 
	 * @Autowired private MockMvc mockMvc;
	 * 
	 * @MockBean private GipherService gipherService;
	 * 
	 * @Before public void setUp() throws Exception {
	 * 
	 * gifList = new ArrayList<>();
	 * 
	 * gipher = new
	 * Gipher("gif001","funnyGif1","gifurl.com","internet","1","Nice Gif", image);
	 * gifList.add(gipher); gipher = new
	 * Gipher("gif002","funnyGif2","gifurl.com","internet","1","Nice Gif", image);
	 * gifList.add(gipher);
	 * 
	 * user = new User("John","john@example.com", gifList);
	 * 
	 * searchList = new ArrayList<>(); searchList.add("searchGif");
	 * user.setSearches(searchList);
	 * 
	 * 
	 * }
	 * 
	 * @After public void teardown() throws Exception {
	 * 
	 * }
	 * 
	 * 
	 */}
